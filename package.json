{
  "name": "mihon-folder-library",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test --test-isolation=none tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
